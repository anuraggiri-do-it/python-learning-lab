# Data Structure

This folder contains data structure references, fundamental implementations, and pattern-based algorithm practice.

## Subfolders

- `algos/` — algorithm practice files
- `basic_ds/` — core data structure implementations like arrays, stacks, and linked lists
- `pattern/` — pattern-based DSA categories such as sliding window, two-pointer, and cyclic sort
